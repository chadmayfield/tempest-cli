---
description: Lint the tempest CLI codebase
command: golangci-lint run
---

# Lint

Run the linter:
```bash
golangci-lint run
```

Run with auto-fix:
```bash
golangci-lint run --fix
```

Vet only:
```bash
go vet ./...
```
